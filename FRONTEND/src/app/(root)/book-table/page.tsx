import ReservationForm from "@/pages/BookTablePage";

const page = (): JSX.Element => {
    return (
        <div className="min-h-screen bg-gray-50 p-4 flex items-center justify-center">
            <ReservationForm />
        </div>
    );
};

export default page;
