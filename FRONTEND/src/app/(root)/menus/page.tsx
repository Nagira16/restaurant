import MenuListPage from "@/pages/MenuListPage";
import React from "react";

const page: React.FC = () => {
    return (
        <div>
            <MenuListPage />
        </div>
    );
};

export default page;
